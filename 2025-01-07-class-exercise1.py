class car:
      def __init__(self, name, engine, remaining_km , number_of_km ):
            self.carname = name
            self.enginename = engine
            self.remaining_kmname = remaining_km
            self.number_of_kmname = number_of_km
      def driving(self):
            if self.number_of_km += number_of_k:
    #there is stg missing!!  

      def refill(self):
            self.refill -= remaining_km

            print("you will have to refill in remainnig_km")

                 





class Server():
    def __init__(self, name, ip, location)
            self.servername = name
            self.ip = ip
            self.location = location
    def boot(self)
          print(f"the srever will be ")        
    def shutdown(self)
my_server = Server ("Server1, "192.168.0.1", "Frankfurt")
print(my_server.name)

my_server.programs = []
my_server.program.append("google")